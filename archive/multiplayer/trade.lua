-- trade.lua — STUB (multiplayer cut for scope reduction)
-- Inter-colony caravan trade was removed to focus on singleplayer.
-- This stub exists so pcall(require, ...) calls don't break.

local NetTrade = {}

function NetTrade.init()      end
function NetTrade.update()    end
function NetTrade.getState()  return nil end
function NetTrade.restoreState() end

return NetTrade
