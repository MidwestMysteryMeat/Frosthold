# Assets removed — licensing

The art (`assets/sprites/`) and audio (`assets/audio/`) that live in this
folder are purchased packs licensed to the project owner only, so they are
**not in the repo**. The game expects the files at these same paths; clones
without them will run with missing-asset placeholders/silence where the
engine allows it. The originals live on the owner's dev machine.
